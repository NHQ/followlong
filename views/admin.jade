h1= title
.content
  - each x, y in channels
    ul #{y}
      - each d in x  
        li #{d}
  h1 Add Channel
    form(method: 'post', action: '/admin')
      input(type: 'text', name: 'channel')
      input(type: 'submit', value: 'Send')